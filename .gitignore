__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/

# Dataset: ignore everything in data/ except the CSV used by the app (needed for Streamlit Cloud).
data/*
!data/melb_data.csv

# Trained artifacts: commit `price_model.joblib` + metrics for deployment (Streamlit has no training step).
# Ignore other experimental dumps if you add them later.
models/experimental_*.joblib
